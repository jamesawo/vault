# Design System

This folder is reserved for future reusable design primitives.

It should eventually hold shared visual building blocks that are clearly derived from the product design work in `design.pen`.

For now, do not move general UI code here unless it is truly reusable and design-owned.
